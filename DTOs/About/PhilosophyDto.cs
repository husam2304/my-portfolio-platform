namespace Portfolio.API.DTOs;

// ── About ─────────────────────────────────────────────────────
public record PhilosophyDto(
    string Label,
    string Title,
    string Description,
    string ImageUrl);
